version: "3.9"

services:
  backend:
    build: ./backend
    container_name: nest_backend
    ports:
      - "3000:3000"
    env_file:
      - ./backend/.env

  frontend:
    build: ./frontend
    container_name: next_frontend
    ports:
      - "3001:3000"